"""
Utility functions and classes used throughout okkie.

You have to import sub-modules of `okkie.utils` directly,
the `okkie.utils` namespace is empty.

Examples::

    from okkie.utils.models import gammapy_build_parameters_from_dict
"""
